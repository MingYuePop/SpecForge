# {{功能名称}} — Task-{{XX}} 手动验收清单

**任务标题**：{{任务标题}}
**对应 AC**：{{AC-XXX, AC-XXX}}
**TDD 状态**：✅ 全部通过（{{N}} 个测试）
**生成时间**：{{YYYY-MM-DD HH:mm}}

---

## 前置条件

> 验证前需要确保的环境状态。用户照做即可进入可验证状态。

- [ ] {{如：开发服务器已启动（`npm run dev`）}}
- [ ] {{如：已登录测试账号 test@example.com}}
- [ ] {{如：数据库中存在测试数据（已由 seed 脚本填充）}}

---

## 验证项

> 每条验证项对应一个可观测的用户行为。按操作顺序排列，前一条的结果状态是后一条的前置条件。

### V-001: {{验证点名称}} ← AC-{{XXX}}

**操作**：{{具体操作步骤，如：打开浏览器访问 http://localhost:3000/login，在手机号输入框输入 13800138000，点击"获取验证码"}}

**预期结果**：
- {{用户能看到的变化，如：按钮变为"60s 后重试"并开始倒计时}}
- {{如果有多个可观测点就列多条}}

**结果**：⬜ 通过 / ⬜ 不通过

**实际行为（不通过时填写）**：
> {{描述实际看到了什么，与预期有何不同}}

**期望调整（通过但效果不满意时填写）**：
> {{功能逻辑没问题，但你期望的效果是什么？如：动画太快了希望慢一点、间距太大想紧凑些、希望有过渡效果等。此项不阻塞任务完成，统一走 feature-evolution 处理。}}

---

### V-002: {{验证点名称}} ← AC-{{XXX}}

**操作**：{{操作步骤}}

**预期结果**：
- {{可观测结果}}

**结果**：⬜ 通过 / ⬜ 不通过

**实际行为（不通过时填写）**：
>

**期望调整（通过但效果不满意时填写）**：
> {{此项不阻塞任务完成，统一走 feature-evolution 处理。}}

---

{{更多验证项按上面的格式继续}}

---

## 验证总结

| 验证项 | AC | 结果 | 期望调整 |
|--------|-----|------|---------|
| V-001: {{名称}} | AC-{{XXX}} | ⬜ | ⬜ 无 / ⬜ 有 |
| V-002: {{名称}} | AC-{{XXX}} | ⬜ | ⬜ 无 / ⬜ 有 |

**总体结果**：⬜ 全部通过 → 任务完成 / ⬜ 存在问题 → 进入修复

**期望调整汇总**：⬜ 无调整需求 / ⬜ 有调整需求 → 作为后续迭代（feature-evolution）输入

**备注（可选）**：
> {{验证过程中发现的额外问题、建议、或需要讨论的点}}